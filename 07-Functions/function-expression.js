//Function expressions — storing functions inside variables.
let add = function(a, b) {
    return a + b;
};

let subtract = function(a, b) {
    return a - b;
};

console.log(add(10, 5));//15
console.log(subtract(10, 5));//5